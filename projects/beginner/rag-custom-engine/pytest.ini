[pytest]
markers =
    integration: calls OpenAI API (requires live server + OPENAI_API_KEY)
    ui: Playwright browser tests (requires live server)

# Run only non-integration, non-ui tests by default
addopts = -m "not integration and not ui" -v
testpaths = tests
cache_dir = tests/.pytest_cache
